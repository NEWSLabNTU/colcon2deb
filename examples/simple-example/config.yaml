version: 1

# Docker configuration
docker:
  dockerfile: ./Dockerfile
  image_name: colcon2deb-simple-example

# Output configuration
output:
  directory: ./build

# Package configurations
packages:
  directory: ./debian-overrides

# Build options
build:
  ros_distro: humble
  parallel_jobs: 4
  install_prefix: /opt/ros/humble
