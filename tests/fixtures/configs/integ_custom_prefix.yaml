version: 1

docker:
  dockerfile: ../Dockerfile
  image_name: colcon2deb-test-custom

output:
  directory: ./build

packages:
  directory: ./debian-overrides

build:
  ros_distro: humble
  parallel_jobs: 2
  install_prefix: /opt/testproject/1.0
  package_suffix: "1-0-0"
